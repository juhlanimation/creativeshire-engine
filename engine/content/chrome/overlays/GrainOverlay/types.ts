/**
 * GrainOverlay props.
 * No configurable props — the overlay renders a fixed film grain effect.
 */

export interface GrainOverlayProps {
  /** Optional CSS class name */
  className?: string
}
