/**
 * ReactSection types.
 * Props interface is defined in meta.ts — re-exported here for
 * architecture test compliance (every section pattern needs types.ts).
 */

export type { ReactSectionProps } from './meta'
