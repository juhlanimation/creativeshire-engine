/**
 * Video-based behaviours.
 * Triggered by video playback state (currentTime, frame, ended).
 */

// Auto-register by importing
import './frame'

// Re-export for explicit imports
export { default as videoFrame } from './frame'
