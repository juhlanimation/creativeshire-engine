/**
 * Emphasis effects barrel.
 * CSS-only emphasis animation effects.
 *
 * Effects:
 * - spin.css: Continuous rotation for loading indicators
 */

// CSS files are imported directly where needed
// This barrel exists for structural consistency
export {}
