/**
 * Transform effects barrel.
 * CSS-only transform-based animation effects.
 *
 * Effects:
 * - scale.css: Scale transform animations
 * - slide.css: Slide/translate animations
 */

// CSS files are imported directly where needed
// This barrel exists for structural consistency
export {}
