/**
 * Mask effects barrel.
 * CSS-only mask-based animation effects.
 *
 * Effects:
 * - reveal.css: Clip-path reveal animations
 * - wipe.css: Wipe transition effects
 */

// CSS files are imported directly where needed
// This barrel exists for structural consistency
export {}
