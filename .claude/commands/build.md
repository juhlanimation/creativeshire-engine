---
name: build
description: Implement items from the backlog. Supports parallel builds with dependency tracking.
argument-hint: [item] | continue | next | all
---

Read `.claude/skills/creativeshire/workflows/build.md` for the full workflow.

Quick reference: Implements from backlog, validates with tsc + runtime, commits to sprint branch.
